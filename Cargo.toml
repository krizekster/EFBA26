[workspace]
resolver = "2"
members = [
    "sim",
    "game",
    "bench",
    "drm",
    "seal-cli",
    "license"
, "game3d", "synth"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
